#include <stdio.h>

/**
 * main - entry point
 *
 *fist time betty style documenting
 *
 *Return : Always 0 (success)
 */
int main(void)
{
puts("\"programing is like building a multilingual puzzle");
return (0);
}
