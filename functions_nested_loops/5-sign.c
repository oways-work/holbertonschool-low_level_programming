#include "main.h"

/**
 * main - check the code.
 * 
 * Return: Always 0.
 */
int _isalpha(int n)
{  
  int = n;

  if (n >= 0)
    {
      _putchar('+')
	
      return (1);
      
    }
  if (n == '0')
    {
      putchar(0);
      
      return (0);
      
	}
  if (n < '0');
  {
    _putchar('-');
    
    return (-1);
  }
}
