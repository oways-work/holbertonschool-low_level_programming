#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int _abs(int n)
{
	if (n >= 0)
	{	
		_putchar(n);
        }
	else if (n < 0)
	{
		_putchar(n * -1);
	}
	return (0)
}
