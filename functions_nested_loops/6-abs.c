#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * this fuction prints the absolute value
 *
 * Return: Always 0.
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}

}
